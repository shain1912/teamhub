import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'kr.co.kodekorea.teamkode',
  appName: 'TeamKode',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
